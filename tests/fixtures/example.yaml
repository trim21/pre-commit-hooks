e: ee                                                        # 122
# 233
 # 233

jobs:
  - template: /.ci/templates/job.yaml #   com
    parameters:
      name: Binary
      pythonVersions: ['3.7']
      images:
        - windows
        - ubuntu
        - "macos"